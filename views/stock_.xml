<odoo>
    <record model="ir.ui.view" id="adding_gln_field">
        <field name="name">Adding GLN Field</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='code']" position="after">
                <field name="gln"/>
                <field name="nve_prefix"/>
            </xpath>
        </field>
    </record>

  <record model="ir.ui.view" id="adding_nve_field">
        <field name="name">Adding NVE Field</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock_delivery.view_picking_withcarrier_out_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_nve_report" string="Print NVE" invisible="not activate_nve or  state != 'done' or picking_type_code!='outgoing' or not result_packages" type="object"/>
            </xpath>
        </field>
    </record>
</odoo>
